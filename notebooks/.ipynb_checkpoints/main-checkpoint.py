'''
Start to layout the bare bones structure of the map data as a fxn
'''

import functions

functions.test_fxn()

# def __main__():
#     find_sites()
#     flow_data = load_time_series(sites)
    
#     for day in [1, 7, 14, 28]:
#         get_most_recent_values(day)
#         flow_data_nday = calculate_rolling_avg(day)

#         percentiles = get_percentiles(calculate_single_day_percentile_thresholds(flow_data_nday[0]))
#         ? percentile_year_count = get_years_used_for_percentile_calcs(percentile_values)
        
#         estimate_percentile_of_most_recent_value -> pd.DataFrame
#         prepare_df_for_graph()
#         create_gage_condition_map